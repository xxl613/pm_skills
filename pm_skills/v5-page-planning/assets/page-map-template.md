# 页面地图 · {{项目名}}

- 来源需求文档：{{相对路径}}
- 生成日期：{{YYYY-MM-DD}}
- 状态：{{🟡提案待确认 ｜ ✅已确认（YYYY-MM-DD，记录用户放行语与闸口决定）}}

> 状态为 🟡 时**禁止生成单页文档**；用户对页面清单明确放行后改为 ✅ 并记录闸口决定，再进入逐页生成。
> 修订过页面清单（改/合/拆）→ 状态退回 🟡，重新给用户过目。

## 页面清单

| 编号 | 页面名 | 平台 | 版式 | 设计目标（一句话） | 文档 |
|---|---|---|---|---|---|
| 01 | {{页面名}} | {{mobile/desktop}} | {{桌面 A/B/C+宽度 · 移动 A-m/B-m/C-m}} | {{目标}} | [页面-01-{{slug}}.md](页面-01-{{slug}}.md) |

## 参数覆盖矩阵

> 每个参数 ID 必须出现一行；"落到页面"为空即为漏接。

| 参数ID | 摘要 | 落到页面 | 状态 |
|---|---|---|---|
| {{REQ-xxxx}} | {{摘要}} | {{01, 03 或留空}} | {{✅已承接 / ⚠️重复待去重 / ❌漏接}} |

## 页面跳转关系

> 页面间的边 = 用户流。下游 v5-ux-rule 据此把触发点渲染成真链接 `<a href="页面-NN-…html">`，
> 让静态原型变成可点击走查（file:// 相对链接即可，无需起服务）。**终点必须是本页面集真实存在的页**，断链=规划失败。
> 左导航是一组**全局边**，单列一次即可，不必每页重复。

| 起点 | 触发点（元素 / 区域） | 终点 | 条件 / 说明 |
|---|---|---|---|
| 全局·左导航 | {{导航项，如「工作台」}} | {{01}} | 各页通用 |
| {{01}} | {{元素，如 助手卡「进入介绍页」}} | {{03}} | {{说明}} |

```
流转简图（ASCII / mermaid 皆可）：
{{01 工作台}} ──助手卡──▶ {{03 介绍页}} ──查看演示──▶ {{04 展示页}}
      └──导航·交小研──▶ {{02 AI首页}} ──@助手──▶ {{03}}
```

## 自检结论

- ❌ 漏接（无任何页面承接，必须解决）：{{列出 ID，或「无」}}
- ⚠️ 重复（多页承接，确认是否有意）：{{列出 ID，或「无」}}
- 🔗 入口/出口自检：{{每页 ≥1 入边（非孤岛）、≥1 出边或明确终点；返回路径齐；断链=失败}}
- 页面总数：{{N}}　覆盖参数：{{已承接数}} / {{总数}}
