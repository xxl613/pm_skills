# Stitch适配

将项目已确认的DESIGN和对应页面PRD翻译为“语义描述 + 具体值 + 功能角色”，按当前可用Stitch工具操作。描述页面任务、优先级、分组、交互状态、字体角色、表面与功能色、适配方式。

不要让Stitch另起一套默认的高变化/高动效风格；传入指定标准的不可变区域和素材，返回后仍用标准基准核对。输出属于设计候选，未经校准不能升级为项目设计真源。不要把生成截图里的新数据当作来源。
